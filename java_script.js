// first example
const text = 'Hello World'
print(text);

// second example
function showWages(rate, days) {
		print("sum: ", rate * days );
	};
const rate = 100;
const days = 22;
showWages(rate, days)